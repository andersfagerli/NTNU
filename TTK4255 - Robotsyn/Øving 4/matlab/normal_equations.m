function [JTJ, JTr] = normal_equations(K, platform_to_camera, p_model, uv, weights, yaw, pitch, roll)
    %
    % Task 1b: Compute the normal equation terms
    %
    JTJ = np.eye(3); % Placeholder
    JTr = [0 0 0]';  % Placeholder
end
