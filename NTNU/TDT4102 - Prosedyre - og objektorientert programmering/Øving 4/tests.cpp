#include "tests.hpp"

void testTablesSorting() {
    int percentages[20];
    //randomizeArray(percentages,20);
    //printArray(percentages,20);
    int b = rand() % 101;
    std::cout << b << std::endl;
}